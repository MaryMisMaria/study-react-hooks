import { Box, Typography } from '@mui/material'

const HooksUseCallback = () => {
  return (
    <Box>
      <Typography variant='h6'> useCallback - this hook </Typography>
      <Typography>
				useCallback
      </Typography>
    </Box>
  )
}

export default HooksUseCallback
